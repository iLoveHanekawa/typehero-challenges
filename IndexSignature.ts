type GroceryList = { [key: string]: number };

type InappropriateActionBySituation = { [key: string]: string[] };

type CharactersById = {
	[key: string]: { id: number; name: string; status: string; species: string };
};
